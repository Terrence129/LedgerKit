# LedgerKit Agent Protocol

本文件适用于整个仓库。它规定 agent 在 LedgerKit 中调查、设计、实现、测试和交付变更时必须遵守的工作方式。

## 开始工作

1. 先阅读本文件和 `docs/README.md`。
2. 再按 `docs/README.md` 的任务路由，只读取当前任务需要的项目文档。
3. 开始编辑前检查工作区状态和现有改动；保留用户及其他 agent 的无关改动。
4. 涉及金额、汇率、投资、冲正、迁移或报表口径时，必须阅读 `docs/financial-rules.md`、相关 Accepted ADR 和对应黄金样例。
5. 如果权威来源相互冲突，停止依赖猜测继续实现，列出冲突和影响并请求用户决策。

## 权威性顺序

同一问题出现冲突时，按以下顺序处理：

1. 用户在当前任务中的明确指令。
2. 本文件及更深目录中的适用 `AGENTS.md`。
3. `docs/adr/` 中状态为 `Accepted` 的 ADR；较新的 Accepted ADR 覆盖被其明确替代的旧 ADR。
4. `docs/financial-rules.md` 与已审阅的黄金样例；二者必须保持一致。
5. `docs/多币种个人账本-开发计划书.md`。
6. `docs/agent-context.md`、操作手册和 README。

`docs/agent-context.md` 是导航和当前状态摘要，不得用来私自推翻 Accepted ADR 或财务规则。

## 不可妥协的产品边界

- 产品是受当前多币种 Excel 账本启发并以其作为迁移与验收基线的本地优先个人财务桌面应用。
- 运行时目标是一个桌面应用进程树、一个活跃的权威 SQLite 账本、零后端、零强制云依赖、零常驻辅助服务和最少第三方依赖。
- 核心功能必须离线可用；任何联网能力必须由用户明确触发或启用，并且不得成为账本可用性的前提。
- WebView/UI 不得获得任意 SQL、任意 posting、任意 shell 或不受限文件系统能力。所有财务写入必须经过权威 Core 的高层业务命令。
- 不为追求体积删除财务正确性、迁移对账、备份恢复、隐私或关键测试。

## 财务与数据规则

- 禁止使用二进制浮点数承担金额、证券数量、单价、汇率、成本或盈亏的权威计算与持久化。
- 财务规则只能在一个权威 Domain/Core 中实现；UI、导入器、SQL 和导出器不得各自复制一套规则。
- 已过账事件不得原地覆盖或物理删除；更正使用可追踪的修订或冲正。
- 业务事件、其确定性 posting 和投影水位更新必须在一个 SQLite transaction 中完成。
- 投影是可重建派生数据；删除投影后必须能从源事实确定性重建。
- 缺失必要汇率或价格时必须明确标记未折算/未估值，禁止按零或 1:1 静默处理。
- Excel 导入先进入 staging，完成校验、问题清单和对账并经用户确认后，才可原子提交到新的正式账本。
- 未解决 `docs/agent-context.md` 中的 M0 blocker 时，不得实现依赖该决策的迁移或财务结果。

## 隐私与公开仓库安全

- 不提交真实账本、数据库、附件、备份、日志、诊断包、私人截图或临时分析输出。
- 不把真实姓名、商户、账号、备注、余额或逐笔交易复制到源码、文档、fixture、测试快照、日志或提交信息。
- 只使用程序生成或人工复核的脱敏小样本；所有金额必须是合成数据。
- 原始 Excel 永不修改。除非任务明确要求调查源文件，否则不要读取或处理它。
- 当前工作站的真实 Excel 定位信息只保存在被 Git 忽略的 `docs/local/private-source-workbook.md`。用户明确授权真实源调查、真实数据微调、最终对账或 cut-over 后，先读取该记录并校验文件名与 SHA-256，再从只读原件创建任务副本；记录缺失或校验失败时停止并请求用户确认。
- 不把 `docs/local/private-source-workbook.md` 中的绝对私人路径复制到任何 tracked 文件、日志、诊断包、fixture 或提交信息。本机记录的存在不覆盖实施提示词中“只使用脱敏 fixture”的限制。
- 任何准备公开的变更在提交前都要检查绝对私人路径、密钥、令牌、电子邮件、账号样式和未忽略的数据文件。
- 不在文档、日志或最终回复中输出凭据。发现疑似秘密时停止公开操作并报告。

## 任务路由

| 任务类型 | 额外必读内容 |
|---|---|
| 产品范围、里程碑、验收 | 开发计划书、`docs/agent-context.md` |
| 金额、汇率、收支、投资、报表 | `docs/financial-rules.md`、相关 ADR、黄金样例 |
| 架构、技术栈、依赖 | ADR 索引、开发计划书技术栈/架构章节 |
| Excel 导入、cut-over、对账 | `docs/operations/migration.md`、相关 ADR、脱敏 fixture |
| SQLite、备份、恢复、升级 | `docs/operations/backup-restore.md`、相关 ADR |
| UI 与支出分析 | 开发计划书页面规格、财务规则、可访问性要求 |
| 发布、打包、公开仓库 | 隐私规则、`.gitignore`、发布与轻量预算章节 |

## 决策与文档更新

- 会改变财务结果、持久化格式、安全模型、技术栈或跨模块边界的选择必须通过 ADR。
- Agent 可以起草 `Proposed` ADR，但不得自行将需要用户选择的 ADR 改为 `Accepted`。
- 只有 Accepted ADR 才能改变 `docs/financial-rules.md` 中的既有规则或黄金预期。
- 改变财务行为时，先更新/增加规则和黄金样例，再实现代码；不得为了让测试通过而擅自改写预期。
- 仅在发生可跨任务复用的持久事实变化时更新 `docs/agent-context.md`；不要把它写成工作日志。
- 操作流程变化写入对应 runbook；不要把实施细节堆进本文件。

## 实现与依赖

- 保持模块化单体和单应用，不引入微服务、远程数据库、本地 HTTP 服务、Node/Python sidecar 或后台 daemon。
- 优先标准库、平台能力和已存在依赖；新增生产依赖前记录用途、体积、许可证、维护状态、安全影响和不用它的成本。
- 前端图表优先原生 HTML/CSS 与语义表格；除非有 Accepted ADR，不增加重量级图表或 UI 运行时。
- 数据库只由权威 Core 访问；若最终采用 Tauri，不向前端暴露 `tauri-plugin-sql` 的通用查询/写入能力。
- 平台相关能力必须隔离在端口/适配器中，不把 Windows 路径、注册表或 WebView 假设写入 Domain。

## 完成前验证

- 运行与改动风险相称的格式、静态检查、单元、集成、黄金、迁移或打包测试。
- 在统一项目检查命令尚未建立前，明确列出实际执行过的检查；不得声称不存在的测试已通过。
- 涉及财务规则时验证正常、边界和失败路径，并检查 posting/投影可重建性。
- 涉及迁移或恢复时只在副本/临时位置演练，验证成功前不得切换或覆盖唯一有效数据。
- 完成回复说明：变更内容、验证结果、仍未解决的 blocker，以及是否更新了 ADR/上下文/规则。
